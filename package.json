{
  "name": "blockchain-book",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:site": "node scripts/build-site.mjs",
    "dev": "npm run prepare:site && vitepress dev docs",
    "build": "npm run prepare:site && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "markdown-it-mathjax3": "^4.3.2",
    "vitepress": "^1.6.3"
  }
}
